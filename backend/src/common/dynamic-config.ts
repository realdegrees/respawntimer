// TODO: save this in db, not at runtime on server
// Config stuff to be changed by the guild's bot manager role
export const dynamicConfig = {
    commandPrefix: '!',
    managerRole: '',
    permissionDeniedResponse: `Looks like you don't have permission to use this command :/`
}