import { Reaction } from '../../common/reaction';
import { notImplementedReaction } from '../default/not-implemented.reaction';

export const audioListReaction = Reaction.create('list', notImplementedReaction);